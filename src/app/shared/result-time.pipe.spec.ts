import { ResultTimePipe } from './result-time.pipe';

describe('ResultTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ResultTimePipe();
    expect(pipe).toBeTruthy();
  });
});
